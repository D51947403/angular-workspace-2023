import { ReverseStringPipePipe } from './reverse-string-pipe.pipe';

describe('ReverseStringPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ReverseStringPipePipe();
    expect(pipe).toBeTruthy();
  });
});
