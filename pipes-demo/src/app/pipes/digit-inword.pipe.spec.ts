import { DigitInwordPipe } from './digit-inword.pipe';

describe('DigitInwordPipe', () => {
  it('create an instance', () => {
    const pipe = new DigitInwordPipe();
    expect(pipe).toBeTruthy();
  });
});
